<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- TTS does NOT require special permissions -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NammaPlatform">

        <!-- Splash / Main Entry -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Station Selection -->
        <activity
            android:name=".ui.station.StationSelectionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Train List -->
        <activity
            android:name=".ui.train.TrainListActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Coach Layout -->
        <activity
            android:name=".ui.coach.CoachLayoutActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Live Train Status -->
        <activity
            android:name=".ui.train.LiveStatusActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>
</manifest>
